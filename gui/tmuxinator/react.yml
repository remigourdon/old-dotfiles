# /home/remi/.config/tmuxinator/react.yml

name: react
root: <%= @args[0] %>

windows:
  - editor:
      layout: main-horizontal
      panes:
        - nvim
        -
  - server:
      layout: even-horizontal
      panes:
        - npx parcel src/index.html
        - 
